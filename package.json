{
  "name": "remote-file-compare",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.58.0"
  },
  "activationEvents": [
    "onCommand:extension.compareFiles"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.compareFiles",
        "title": "Compare Files"
      }
    ]
  },
  "dependencies": {
    "axios": "^1.6.2",
    "ssh2": "^1.15.0",
    "vsce": "^2.15.0"
  },
  "scripts": {
    "pack": "vsce package"
  },
  "repository": {
  }
}
